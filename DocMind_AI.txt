pip install fastapi uvicorn python-multipart
pip install pymupdf. used to read, extract, modify, and create PDF documents. compared to other it can extract images, edit pdf, speed, 
pip install langchain-text-splitters to do chunking
pip install sentence-transformers to install embedding models

in python list we loop through every embeddings and compare, does brute-force search.
Chroma builds vector space and has vector index, saves in memory and searches embedded vector in the vector space.
Chroma also compares embeddings but it measures similarity. Chroma avoids comparing against every embedding.
Chroma uses vector index. python list will disappear after programming because it stores in ram.
Chroma itself stores metadata storing, in python we build it

Gemini is not reading the PDF or embeddings to name the cluster. It is only seeing the filenames.
ChromaDB is the component that searches the embedding vectors.
Then Gemini gets the chunks
Gemini does not search the vectors.
Gemini does not calculate which chunks are relevant.
ChromaDB does the retrieval.
Gemini reads the retrieved text chunks and generates the natural-language answer.